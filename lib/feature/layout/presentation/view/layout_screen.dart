import 'package:flutter/material.dart';

class LatoutScreen extends StatelessWidget {
  const LatoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
